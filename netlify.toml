[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: let React Router handle client-side routes
# (without this, refreshing /login or any deep link returns Netlify's 404)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
